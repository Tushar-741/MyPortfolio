# My Portfolio (React + Tailwind)

This is a minimal Vite + React + Tailwind portfolio scaffold built from the provided resume data.

Quick start:

```bash
npm install
npm run dev
```

Notes:
- Replace placeholder URLs in `src/data/resume.js` with your real GitHub/LinkedIn/coding profiles.
- This project uses Vite for development.
